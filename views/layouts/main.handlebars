<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title> 
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <header>
    <!-- Navigation Links -->
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/dashboard">Dashboard</a></li>
        {{#if isLoggedIn}}
          <li><a href="/logout">Logout</a></li>
        {{else}}
          <li><a href="/login">Login</a></li>
        {{/if}}
      </ul>
    </nav>
  </header>

  <main>
    <h1>{{header}}</h1> <!-- Dynamic header -->
    {{#if posts.length}}
      <section class="blog-posts">
        {{#each posts}}
          <article class="post">
            <h2>{{title}}</h2>
            <p>{{content}}</p>
            <p>Posted by: {{author}}</p>
            <time datetime="{{createdAt}}">Date: {{formatDate createdAt}}</time>
          </article>
        {{/each}}
      </section>
    {{else}}
      <p>No blog posts yet.</p>
    {{/if}}
  </main>

</body>

</html>